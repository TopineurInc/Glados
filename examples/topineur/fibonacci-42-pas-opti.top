package main

|- Naive recursive Fibonacci WITHOUT optimization

def fib(n: Int): Int {
  if n <= 1 then top n
  else top fib(n - 1) + fib(n - 2)
}

def main(): Int {
  let f: Int = fib(42)
  println("fib(42) = " ++ show(f))
  top 0
}
