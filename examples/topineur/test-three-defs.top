package main

def add(a: Int, b: Int): Int {
  top a + b
}

def sub(a: Int, b: Int): Int {
  top a - b
}

def mul(a: Int, b: Int): Int {
  top a * b
}

def main(): Int {
  let x = add(5, 3)
  let y = sub(10, 4)
  let z = mul(2, 6)
  println("Results: " ++ show(x) ++ ", " ++ show(y) ++ ", " ++ show(z))
  top 0
}
