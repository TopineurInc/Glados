name: CI

on:
  push:
    branches:
      - '*'

jobs:
  build:
    runs-on: self-hosted

    steps:
      - uses: actions/checkout@v4
      - name: Run script
        run: echo "✅ CI triggered on branch ${{ github.ref_name }}"